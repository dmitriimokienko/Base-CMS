{
  "name": "base-cms",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "update:version": "lerna version --no-push"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}